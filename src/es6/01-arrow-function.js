//Declaracion funcion

//declaracion normal
function square(num) {
    return num * num;
}

//declaracion en variable con funcion flecha
const square = (num) => {
    return num * num;
}

//declaracion en variable con return implicito
const square = (num) => num * num;